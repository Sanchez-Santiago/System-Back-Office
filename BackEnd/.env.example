PORT=8000

# ============================================
# CONFIGURACIÓN POSTGRESQL/SUPABASE (REQUERIDO)
# ============================================

# URL completa de conexión (formato: postgresql://user:password@host:port/database)
POSTGRES_URL=postgresql://postgres:password@localhost:5432/system_back_office

# O configurar por partes (se usa si POSTGRES_URL no está definida)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=system_back_office

# Variables alternativas (fallback para compatibilidad)
DB_HOST=${POSTGRES_HOST}
DB_PORT=${POSTGRES_PORT}
DB_USER=${POSTGRES_USER}
DB_PASSWORD=${POSTGRES_PASSWORD}
DB_NAME=${POSTGRES_DB}

# Supabase (opcional - usar si se despliega en Supabase)
SUPABASE_URL=your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ============================================
# CONFIGURACIÓN DE CONEXIÓN (IMPLEMENTADO)
# ============================================

# Timeout para conexiones (ms) - implementado en PostgreSQL.ts
DB_CONNECTION_TIMEOUT=10000

# Número de reintentos de conexión - implementado en PostgreSQL.ts
DB_CONNECTION_RETRIES=3

# ============================================
# SEGURIDAD Y AUTENTICACIÓN (REQUERIDO)
# ============================================

# Modo: development | production
MODO=development

# Orígenes permitidos para CORS (separados por coma)
# Ejemplo: https://tu-dominio.com,https://www.tu-dominio.com
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Clave secreta para JWT (¡CAMBIAR EN PRODUCCIÓN!)
JWT_SECRET=tu_clave_secreta_muy_segura_aqui_cambiar_en_produccion

# Algoritmo JWT (HS256 | HS384 | HS512 | RS256 | etc)
# Implementado en AuthService.ts
JWT_ALGORITHM=HS256

# ============================================
# NOTAS
# ============================================
# - Copiar este archivo a .env y configurar los valores reales
# - Nunca subir .env a git (está en .gitignore)
# - En producción, usar variables de entorno del servidor
# - Las cookies httpOnly se manejan automáticamente
# - El token JWT se renueva automáticamente
